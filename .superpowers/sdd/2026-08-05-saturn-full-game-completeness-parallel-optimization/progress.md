# SDD ledger — plan: docs/superpowers/plans/2026-08-05-saturn-full-game-completeness-parallel-optimization.md

Workspace: `D:/Code/RetroDev/sm64-saturn-port/sm64-port/.worktrees/sh2-native-math-purge`.
Controller preflight, 2026-08-05: the plan was read in full; no internal
requirement conflict was found. This worktree is isolated from the main branch.
The pre-existing dirty `tools/saturn/verify_sh2_native_math.py` and
`tools/saturn/test_verify_sh2_native_math.py` changes are the Task 1 candidate
implementation. The older `2026-08-03-saturn-overlapped-render-pipeline`
ledger/brief and all existing untracked logs, audits, captures, routes, and
MSYS scratch remain unrelated and must not be staged, rewritten, or deleted.

Task 1: active — base `9c577aa1f9657f207eb57e8a0ebf6041291acc18`; dispatching
one implementer to audit, complete, and test only the dirty native-math
verifier/oracle repair. No host, linked-image, target, Ymir, FPS, or manual
gate is credited before its required evidence and independent review.

Task 1: design correction — the tracked `sh2_native_math_route_oracle_v1.txt`
still names removed pre-descriptor-queue workers, while the exact ELF/source
route uses render-job runtime callbacks. Treat the oracle text and a directly
coupled tracked digest fixture, if required by its format, as Task 1 inputs;
update them truthfully rather than filtering stale declarations in Python.

Task 1: review failed — Critical: the BOB route proof accepts a mutation that
rewrites BOB to CCM before `lvl_set_current_level`, so it can suppress calls on
a non-BOB route. Important: the renderer oracle inventories a declared
callback table without proving the exported function returns and runtime uses
that table, and its start/poll roots are not body-scoped. Review details:
`task-1-review.md`. Fix round 1/5 dispatched; prescribed Qt wrapper and capped
full-verifier gates remain open and are not reclassified.

Task 1: fix round 1/5 implemented — commit `52c7542224e4453554f563f5fd2e9c0e4387e451`
adds permanent RED/GREEN fixtures for BOB-to-CCM route mutation, exact ELF
identity mismatch, inactive returned callback table, detached runtime
activation, and a lifecycle-start decoy outside the start root. The focused
suite is 222/222 green; mutation and equivalent MSYS normal host gates are
green. The prescribed Qt wrapper quote defect and prior broad exact-ELF
timeout remain open. Scoped re-review pending.

Task 1: source-complete — rereview of `f349fe9f..52c75422` is PASS with both
the Critical BOB-route/ELF-identity and Important active-callback/root-body
findings resolved and no new C/I/M findings. Evidence: focused `222/222`,
prescribed mutation green, and equivalent MSYS normal host green. The exact
Qt wrapper failure and prior `600.7s` broad exact-ELF timeout remain unchecked;
the broad Task 10 host gate is runnable but not complete. No linked target,
Ymir, FPS, hardware, or manual evidence is credited. Transition documentation
was reconciled in the active plan and inherited Task 10 plan/evidence report.

Task 2: active — base `52e4ec9f0b649635c25114d049d78cecf03948d2`.
The implementation brief was generated from the active plan. One integration
spine implementer will archive the accepted A9A ELF/ISO/CUE without rebuilding
it, then add fixed-width feature/config/package identity generation and
capture validation. Task 1 remains source-complete with its broad exact-ELF
gate explicitly open; no Task 2 work may treat that gate as green.

Task 2: review failed — Critical: compiled identity/effective-config hash omit
compiler-affecting wrapper values (including ATAN2, demo/view, slave,
camera-probe, BSP-flat/trace controls), allowing identity-label collisions.
Important: archive evidence accepts arbitrary cadence/identity/profile claims;
Minor: conflicting-manifest failure can leave partial archive files. Exact A9A
trio hashes/ancestry, ABI identity fields, compiled feature flags, and capture
loaded-target check were verified. Fix round 1/5 dispatched to the original
implementer; no target/Ymir/native-math claim is reclassified.

Task 2: source-complete — fix rereview of `264f3b4b..5008759e` is SPEC PASS
and QUALITY PASS. Critical identity coverage, Important evidence binding, and
Minor archive transaction ordering are all resolved with no new C/I/M. Final
serial host evidence: identity 9/9, sourceboot identity 5/5, capture 35/35,
archive 12/12, plus exact no-build A9A revalidation. Commits: `f5a0248d`,
`264f3b4b`, `8c97fd4e`, `5008759e`. Linked SH-2 symbol/layout, Ymir/manual,
and broad Task 1 native-math gates remain unchecked; no target evidence is
credited. Active-plan steps and status reconciled before releasing Task 3.

Task 3: active — base `ce9f7ef3a4f7a4a389327b22ad1796a155c07e5f`.
One bounded tooling implementer will create the source-hash-backed transitive
BOB closure generator and reviewed computed-spawn-rule seam. It may run only
the named Python host tests and target-creation command; no linked target,
Ymir, manual, or FPS evidence is in scope. Tasks 1 and 2 remain
source-complete with their stated open gates preserved.

Task 3: review failed — Critical: collector silently omits reachable native
computed spawn/effect edges; current deterministic 53-record/59-hash BOB
output is not authoritative. Important: `LOAD_MODEL_FROM_GEO`/geo provenance,
cycle semantics, act-compatible maxima, required schema fields, rule-edge
integrity, area scoping, source-derived audio declarations, and
checkout/newline-independent output identity all need repair. Minor:
non-repository rule hashes must fail closed and real-edge/totals tests are
needed. Fix round 1/5 dispatched; host-only results and untracked output are
preserved, while no target/Ymir/FPS/native-math evidence is credited.

Task 3: fix round 1 rereview failed — direct-native detector misses reachable
helper/action/respawner/particle and computed-table edges, including Koopa
shell, exclamation-box, and respawner dependencies. Important: source-attested
rule/count/capacity/global uniqueness, repository rule-file hash, LevelScript
area scope, function/behavior-scoped audio bank/ID provenance, and real-edge
tests remain incomplete. `LOAD_MODEL_FROM_GEO`/geo provenance, static cycles,
per-act accumulation mechanics, mandatory schema fields, and canonical bytes
are repaired. Named Task 3 host gates are green but insufficient. Fix round
2/5 dispatched; no target/Ymir/FPS/native-math evidence is reclassified.

Task 3: repair round 2 partial — commit `19135371` adds source-attested
Koopa-shell/exclamation-box/water-splash coverage and reaches 71 records/71
hash-covered sources with named focused gates green. The implementer explicitly
reports that bounded helper/action/particle/respawner traversal, external-rule
hash enforcement, requested-area isolation, and behavior-scoped
`SOUND_ARG_LOAD` audio derivation remain incomplete. This partial result is not
submitted for rereview or credited; the same repair round continues.

Task 3: repair round 2 partial continuation — commit `9b1539f1` scopes inline
objects to the requested AREA plus local `JUMP_LINK` scripts, with existing
Task 3 tests green. It still lacks the interprocedural native scanner,
source-expression/count/capacity rule attestation, and behavior-scoped audio.
It is not submitted for review or credited; controller narrowed the remaining
implementation into an atomic scanner slice before the rest of this repair.

Task 3: repair round 2 scanner slice — commit `d171dfef` adds bounded
64-body helper traversal from generated `CALL_NATIVE` callbacks, source rules
for newly discovered coin/star helper edges, and a fail-closed unknown-computed
spawn regression. Real BOB reaches zero unruled concrete sites; scene 5/5,
BOB 1/1, and serial compile are green. It is a required subcomponent only:
rule-file source-expression/count/capacity/hash integrity and behavior-scoped
audio provenance remain unimplemented, so no rereview/credit is granted.

Task 3: repair round 2 implementer handoff — the original implementer could
not safely complete source-expression/count/capacity rule attestation and its
mutation suite within its turn after partial duplicate/water-bomb correction.
No unreviewed completion is claimed. A fresh stronger implementer is assigned
only the remaining rule-integrity and audio-provenance slices in the shared
worktree; it must preserve controller docs and legacy dirt.

Task 3: fix round 2 final rereview failed — Critical: native callback index is
limited to behavior `.inc.c` files, omitting cross-file respawner and particle
creation; real 76/78 output excludes `bhvRespawner` and
`bhvWhitePuffExplosion`. Important: model/geo/animation source hashes,
source-attested live capacity, recursive area/local-music scope, schema
referential/aggregate integrity, and real regressions are incomplete; audio
coverage follows the same limited graph. V2 rules/deterministic bytes and
narrow tests are preserved but uncredited. Repair round 3/5 is assigned to a
fresh stronger implementer; no target/Ymir/FPS/package/native-math evidence is
reclassified.

Task 3: fix round 3 rereview failed — Critical: discovery still only scans
`src/game` and multi-model behavior roots collapse; real White Puff misses
bubble provenance. Important: generic helper audio leaks unrelated constants,
schema provenance/typed-list checks are bypassable, BehaviorScript per-site
capacity/Goomba proof and entry JUMP_LINK/comment music rules are incomplete.
The 86/127 host-only result is retained but uncredited. Per round 4/5 policy,
a fresh strongest implementer is assigned; no target/Ymir/FPS/package/native-
math evidence changes.

Task 3: fix round 4 final rereview failed — Critical: ambiguity handling
rejects only syntactic direct calls, silently skipping reachable data/table
dispatch symbols. Important: audio call-site collection accepts `SOUND_*` in
arbitrary non-audio calls. All other round-four contracts/tested BOB facts are
resolved but uncredited. Final repair round 5/5 is assigned to a fresh
strongest implementer with permanent independent regressions; target/Ymir/FPS/
package/native-math evidence remains unchanged.

Task 3: source-complete — final round 5/5 rereview PASS with C0/I0/M0.
Commits `ed863d03`/`43b5f36a` close ambiguous reachable data/table/function
references and non-audio SOUND argument leakage. Final host evidence is
generic 19/19, BOB 1/1, compatibility 1/1, and serial compile exit 0; BOB is
86 records/133 hashes with 54 SFX IDs across 8 banks. Task 4 may now start;
target/Ymir/FPS/package/native-math/manual evidence remains open.

Task 4: active — base `11468a3a3a5ffa7d6990b39d91182e37b8f17bf9`; implementation `ba40126c` delivered focused GREEN (schema 8/8, determinism 3/3, serial provisional target 3/3, package SHA `84A1716C32A1563443EF00DECEF2BEEBC3C7581F113D56298F9D862522BF1F6E`). Independent review is SPEC/QUALITY FAIL with six Important repairs: canonical dependency-mask lowering/limit and nonzero-mask tests; global stable-ID uniqueness; payload-aware header emission; missing dependency/payload mutation tests; shared ABI header generation; and explicit provisional budget semantics. The optional broad BobMeshIR suite timed out after two passes under the CPU guard and remains non-gating. Repair round is active; task remains unchecked/source-incomplete.
Repair round `a3e22842` addresses all six findings: canonical stable-ID dependency-mask lowering with a 32-record bound and nonzero-mask determinism; global stable-ID uniqueness; payload-aware header emission; payload/dependency mutation coverage; one shared generated ABI/types header; and explicit provisional `NONE`/non-residency budget rationale. GREEN: schema 12/12, determinism 3/3, `py_compile`, and serial DLL-preflight provisional-package target exit 0 (16.0s). Independent rereview remains required; task is still unchecked.
Independent rereview of `ba40126c..a3e22842`: SPEC PASS / QUALITY PASS, C0/I0/M0. Task 4 is source-complete and its plan checkboxes are now reconciled. Final BOB/WF package reseal, target boot, Ymir/FPS, native-math, and manual evidence remain with later owning tasks; the broad BobMeshIR timeout remains explicitly non-gating.
Task 5: active — base `6e9190877fc66af7429f66bf653cc3c7aa8535b3`; brief generation pending. Scope is bounded scene-package validation/residency lifecycle only. No target boot or final BOB root claim is permitted in this transition.
Task 5 implementation `422096a1` delivered pointer-free S64P decoding, target-side SHA-256 validation, atomic root/feature-active payload staging, exact-generation retention/retirement hooks, capacity/rollback/eviction/double-commit/zero-section fail-closed behavior, and provisional target rejection. Serialized C runtime/residency gates pass; neutrality/sourceboot policy is 5/5. The broader Snapshot Make gate compiled but ended on the inherited quoted-executable EOF; its produced binary and direct source-policy test passed, so that Make gate remains explicitly non-green. Design correction: runtime stages identities/capacity only and never assumes or writes raw 4 MiB DRAM-cart placement; concrete destination movement remains with later producer/residency tasks. Independent review pending; Task 5 remains unchecked.
Independent review of `422096a1`: SPEC/QUALITY FAIL. Critical: section-offset underflow can read past the supplied package; residency load/commit retains only masks/hashes and no owned root/payload bytes. Important: consumer-retention bits are not connected to render/VDP1/actor-animation/audio lifecycles and can be prematurely cleared; cross-generation alignment ignores absolute high-water; SOURCE.DAT prefix/high-water and provisional rejection are not active sourceboot behavior; C/Python ABI acceptance differs; capacity tests omit SOUND_RAM/scratch/exact-fit/cross-generation cases. Repair round is active; Task 5 remains unchecked.
Repair `c7d6e148` addresses the review findings: safe resealed-offset rejection; owned root/feature-active payload spans with commit-time rehash/rollback; absolute aligned two-generation placement; refcounted snapshot/VDP1/actor/audio adapters; optional linked-root boot caller preserving output on rejection; aligned post-SOURCE.DAT CART span; C/Python UTF-8/generation-zero/zero-byte parity; and root/SOUND/scratch/exact-fit/cross-generation tests. Prescribed serialized C gates and neutrality/sourceboot 6/6 are green. Target build/Ymir/manual remain open; independent rereview required.
Self-audit follow-up `eb99c47c` adds the explicit `<string.h>` freestanding declaration required by the source-cart caller and updates CHANGELOG/report; neutrality remains 6/6. Rereview scope is now `422096a1..eb99c47c`.
Rereview found one remaining Important lifecycle defect: aggregate consumer refcounts allowed duplicate release through a reused handle, so two live refs could be decremented twice and unload early. All prior OOB/ownership/alignment/sourceboot/ABI/capacity findings otherwise appear closed. Final repair round is active; add bounded per-consumer handle tokens and two-live-handle/double-release tests before rereview.
Final lease-token repair `26986d94` replaces aggregate-only retirement with bounded exact consumer handles; duplicate/stale releases cannot decrement another lease. Added two-live-handle, duplicate-release, stale-generation, zero/exhaustion regressions. Focused serial C gates and neutrality 6/6 are green; independent rereview of `422096a1..26986d94` is active. Task 5 remains unchecked.
Independent rereview of `422096a1..26986d94`: SPEC PASS / QUALITY PASS, C0/I0/M0. Task 5 is source-complete and its individual plan steps are reconciled. Open gates remain the inherited Snapshot Make quoting defect, target link/memory inspection, final BOB/WF roots, Ymir/FPS, broad native-math, and manual evidence.
Task 6: active — base `e1d5ff23a46d66c4dcb9effbbe934d9cb2b1a4e8`; brief generation pending. Scope is PCM protocol v2 protected control capacity and serial host/68K model gates only; no semantic audio policy/VM or target/Ymir claim.
Task 6 implementation `7bb8f87f` delivered protected v2 control/SFX rings, producer-last publication and telemetry, control-first 68K consumption, historical v1 preservation, soundtest migration, and linked image/map verification. Green: v1/v2 ABI/transport/publication/model/heartbeat/soundtest gates, linked image/zero-map, PCM mutations 12/12, `py_compile`, and scoped diff. The exact Qt `mingw32-make` spelling remains open only for the inherited unmatched-quote `.exe` launch; DLL-preflight/MSYS equivalent is green. Broad test-tools timed out at 120.7s after early/BOB passes; affected image class rerun 12/12. No target/Ymir/audible claim. Independent review pending; Task 6 remains unchecked.
Independent review of `e1d5ff23..7bb8f87f`: SPEC PASS / QUALITY PASS, C0/I0/M0. Task 6 individual steps are reconciled; exact Qt wrapper and broad timeout remain unchecked/open. Task 7 will start only after the plan transition commit.
Task 7: active — base `68dacb5caa0c1fd3ad069b34eee4185815785c59`; brief generation pending. Scope is the complete compact Mario animation bank and differential host proof only; no actor runtime, target/Ymir/manual, or final package claim.
Task 7 implementation `902ada8a` delivered the compact 209-ID/193-source animation bank and C pose ABI. Green: actor_source 8/8, actor_bank 6/6, existing MarioActorPoseTests 17/17, DLL-preflight `verify-actor-pose-bank`, exact 30 idle + 77 walk differential frames with zero pose/light mismatches. Payload is 596,896 bytes (SHA `a1969781426d4fe9e44a56774f17125fe855050667951096c8b55c35a1f376de`), 20 joints/424 verts/644 primitives/31 meshlets, scratch 3,928. Combined Make remains open only at inherited MSYS quote launch of old meshlet exe; direct fixture and invalid-span mutation pass. `switch_variant_geometry_complete=false`; Task 10 owns branch/state cutover. No runtime/target/Ymir/S64P-final claim. Independent review pending; Task 7 remains unchecked.
Independent review of `68dacb5c..902ada8a`: SPEC/QUALITY FAIL, C0/I2/M1. Important: C actor-bank validation does not check packed material/meshlet/primitive/ref spans, count cross-checks, ownership/index/tier references, or scratch lower bound; compiler provenance accepts arbitrary sorted 64-character hashes instead of the exact 193-file set/digest/payload binding. Minor: checked-in schema omits several headline invariants. Repair round is active; Task 7 remains unchecked.
Repair `68f9dd10` hardens full GEO1 spans/counts/ownership/material/meshlet/primitive/ref/scratch validation and binds exact 193-file provenance, digest, and expected source identity through the C API. Actor_source 8/8, actor_bank 7/7, MarioActorPoseTests 17/17, DLL-preflight pose-bank, direct meshlet, and invalid-span mutation gates are green. Schema is explicitly advisory; executable validation is authoritative. Switch-variant geometry remains false/Task10-owned; independent rereview pending.
Rereview of `902ada8a..68f9dd10`: SPEC/QUALITY FAIL, C0/I2/M0. Remaining Important findings: GEO1 C validation accepts in-range but internally inconsistent material/meshlet/tier/primitive/vertex/source-ordinal relationships and unchecked node/branch metadata; compiler provenance requires 193 self-consistent filenames but does not pin the repository's actual canonical 193-file set, so a resealed renamed set can pass. Final focused repair is active; Task 7 remains unchecked.
Second repair `9ae757d5` pins the actual canonical 193-path inventory (digest `2d7c66e9…`) and adds in-range relationship validation/mutations for material/meshlet/tier/primitive/vertex/source/node/branch/part metadata. GREEN: actor_source 8/8, actor_bank 8/8 including resealed `01`/`02_03` repartition rejection, MarioActorPoseTests 17/17, native-path DLL-preflight C compile with `-Wall -Wextra -Werror`, and actor-pose fixture. Direct MSYS Make spelling has a path-conversion caveat and remains unclaimed; independent rereview active.
Final rereview residual: one Important remains—tier-0 primitive runs are locally checked but not proven as one global gap-free partition, and triangle `d == c` does not require distinct `a/b/c`. Implement coordinated global cursor/distinctness mutation and rerun/re-review before closing Task 7.
Residual repair `b573b6bb` adds a global tier-0 source cursor/final primitive-count coverage across all meshlets and distinct A/B/C triangle enforcement; the coordinated duplicate/gap mutation now fails closed. Focused actor_source 8/8, actor_bank 8/8, Mario pose 17/17, and native-path DLL-preflight C11/Werror/actor-pose gates are green. Scoped rereview is active; Task 7 remains unchecked.
Final rereview record `8929b2c9`: SPEC PASS / QUALITY PASS, C0/I0. Task 7 individual steps are reconciled. Open caveats are the path-conversion/inherited meshlet Make issue, Task 10 switch-variant/runtime cutover, and all target/Ymir/manual/FPS/final-package gates.
Task 8: active — base `7485ad311ed64d17b62be4b53972ff694c5326b7`; brief generation pending. Scope is differential proof of a state-only source-geo seam; if no exact bounded seam exists it must be recorded blocked/deferred without speculative suppression.
Task 8 implementation/evidence `a9c3f9c5` is blocked/deferred by the required differential contract: old root suppression changes graph-owned animation, painting, water, moving-texture, camera/matrix/object, lifecycle, and visibility digests, so no runtime seam was activated and full geo remains authoritative. Warp remains equal only because its transition path is outside the guard. GREEN: source contract 8/8 and serial DLL-preflight C11/Werror `verify-source-geo-state-diff`; combined source-render-policy gate remains open on inherited sourceboot Make identity/default issues. Independent review pending; Task 8 must remain unchecked/blocked until review confirms evidence.
Independent review of `7485ad31..a9c3f9c5`: SPEC/QUALITY FAIL, C0/I2. The production path is safely deferred, but the central C fixture is tautological synthetic field mutation rather than execution of the real normal/suppressed `geo_process_root()` boundary, and the RED only proves missing-file/header setup failure. Repair must narrow report/CHANGELOG claims to static source audit plus illustrative digest model, leave the real differential checklist unchecked, and keep Task 8 blocked/deferred; no runtime optimization may be enabled.
Repair `3110c3de` qualifies all evidence: Python is a static source audit, C is an illustrative digest model that never calls `geo_process_root()` or original callbacks, Make reports the real graph differential open, and CHANGELOG/report state no seam is proven or enabled. Full geo remains authoritative; API is uncalled. Focused static audit 8/8 and DLL-preflight C11/Werror contract pass. Independent rereview required; Task 8 remains blocked/deferred and unchecked.
Independent rereview of `a9c3f9c5..3110c3de`: SPEC PASS / QUALITY PASS, C0/I0/M0. Task 8 is reconciled as blocked/deferred with real differential and combined render-policy gates explicitly open; no seam or suppression is enabled. Task 9: active — base `db7c9569c089afcccbd4551f58ff3e6d642bd544`; brief generation pending.
The generic S64P schema/provisional root must remain separate from Task 22's
final BOB link/reseal owner. Host/package tests only; no target/Ymir/broad gate.
Task 9 implementation `9a904766` delivered source-authoritative audio policy/spatial semantics with exact public APIs, generation-tagged pointer tokens, persistent discrete SFX, per-bank priority/promotion, and single semantic symbol ownership. Green: exact serial DLL-preflight `verify-audio-policy` + `verify-audio-spatial`, Python ABI/ownership/pointer-free 4/4, and host C11/Werror syntax compile. No sequence VM/MC68000 voice scheduler, target/Ymir/manual/FPS, or final audio package claim. Independent review pending; Task 9 remains unchecked.
Task 9 repair/review wave landed through `f12e2ee6` (commits `84fedde2`, `a5d14609`, `003a7637`, `6da2aa0b`, `9a0bc8fa`, `1f71656f`, `80672126`, `f12e2ee6`). Repairs cover source-faithful tick-time request admission, same-frame control ordering, discrete completion/preemption, catalog bounds, jingle guard timing, lowering/global-fade semantics, generation-safe pointer-token retirement, moving-source refresh identity, pending-token lifetime, and cross-bank shared-position isolation. Final independent rereview: SPEC PASS / QUALITY PASS, C0/I0/M0. Fresh serial DLL-preflight `verify-audio-policy verify-audio-spatial`, Python `test_full_game_audio_source.py` 4/4, strict feature-on C11/Werror syntax, and scoped diff checks pass. Task 9 is source-complete and its individual plan steps are reconciled; transport binding, MC68000 sequence/voice service, package residency, target/Ymir/manual/FPS remain explicitly open. Task 10 is now active against the reconciled base.
Task 10 implementation `ea6a1236` and hardening `a4d00a61` are source-complete. The compact 209-ID evaluator is selected from source state, guarded by `SATURN_FEATURE_COMPLETE_MARIO_ANIMATION`, and exercised through the same production meshlet path by the non-promotable sweep harness. Final rereview: SPEC PASS / QUALITY PASS, C0/I0/M1; the minor ABI note is that `pose_slot` grows the shared selector struct and all peers compile against the same header. Repairs add int64 checked translation sums, fail-closed matrix multiplication, two-slot immutable pose handoff for frame overlap, cached diagnostic hashes, and legacy walking/neutral references isolated to feature-off compatibility. Fresh green: DLL-preflight `verify-actor-pose-bank`, feature identity 5/5, variant identity 2/2, sweep validator 2/2, strict feature 0/1 bridge syntax, and scoped diff. The linked variant dry-run stops at missing `YAUL_INSTALL_ROOT`; no ELF/CUE/ISO, target, Ymir, or manual evidence is claimed. Task 10 individual steps are reconciled; Task 11 is now active.
Task 11 implementation `52999c35` and repairs `acff11a8`, `7f0caa62` are source-complete for generic actor-family compiler/ABI scope. The final bank is 47 deterministic S64F families, 99,105 bytes, SHA `97dc231ba1778e815423d05e9c69a13ab7567a83faa1714c085dac3dc69d45f0`, from 86 BOB closure records/133 source hashes. Thirteen unsupported required geo nodes (12 `GEO_CULLING_RADIUS`, 1 `GEO_BRANCH_AND_LINK`) remain explicit and keep `complete_closure=false`. Repairs align Python/C total-mask ranking, canonicalize family representative/order, sum deduped live multiplicity, bind nonzero expected S64F SHA-256 in C, and reject zero/unknown flags or tampered payloads. Fresh green: generic 4/4, full-game source 4/4, serial compile-actor-banks, strict C11/Werror syntax, executable C tamper/hash/ranking tests, and diff-check. Final independent rereview of `52999c35..7f0caa62`: SPEC PASS / QUALITY PASS, C0/I0/M0. S64P linkage, runtime cutover, target/Ymir/manual/FPS remain open. Task 11 individual steps are reconciled; Task 12 is now active.
Task 12 repair wave through `ac3b91b2` preserves a hardened subset: S64A C/Python ABI parity, generated-byte parsing, signed PCM8, package/chunk SHA, portable paths, seq00 missing-asset fail-closed behavior, byte-framed dependency hashes, work/scratch spans, cross-generation disjointness, AIFF MARK/INST and tuning metadata, descriptor overlap rejection, and malformed m64 fixture coverage. Fresh Python 4/4 and direct C residency/overlap gates pass. Final independent rereview remains SPEC/QUALITY FAIL: the real expanded seq00 payload is absent, BOB/WF are music-only and lack closure-selectable S64A/S64P bindings, active-plan validation is incomplete in the 68K accept path, and general m64 control-flow parsing is not complete. Task 12 is blocked/source-incomplete; no full catalog, final S64P, target/Ymir/manual, or audio-completeness claim is made. Task 13 may proceed independently.
Follow-up `b937456e` makes MC68000 acceptance validate both active and replacement plans and treats zero-length spans as non-occupying for cross-generation disjointness. This closes that narrow safety repair, but does not change the blocked status: missing real seq00, absent final S64P/closure-selectable BOB/WF payloads, and incomplete general sequence-control-flow parsing remain.
Task 13 implementation `33e06fb8` and repairs `280e1804`, `f6e0aa03`, `0d6707c8`, `d2e5b604`, `a888ff00` are source-complete. The final independent rereview is SPEC PASS / QUALITY PASS, C0/I0/M0. Focused serial DLL-preflight gates `verify-bob-bsp-header`, `verify-scene-admission`, `verify-portal-windows`, `verify-render-clusters`, and `verify-ztreme-frustum` pass. The production sourceboot link and generated BOB metadata path are covered; target link, Ymir, manual, and FPS evidence remain open. Task 14 is now active.
Task 14 implementation `56c33d76` and repairs `7cb28651`, `508a8da1` are hardened but remain source-incomplete. Final rereview of `7cb28651..508a8da1` is SPEC/QUALITY FAIL, C2/I3/M0: cached snapshot payload writes do not match P2 publication, legal source pool slots above the 64-live observer bound are still discarded/partially published, pre-acquire failure cleanup can strand a bank, observer generation is not tied to skip-zero frame generation, and the LWRAM assertion is a physical ceiling rather than a package reservation. Focused serial actor snapshot/render-bank/queue/batch host gates pass, but unresolved registry fields intentionally keep nonzero production actor admission fail-closed; target sourceboot, Ymir, manual, and FPS remain open.
Task 16 infrastructure `0549f7af` is source-incomplete after independent SPEC/QUALITY FAIL review. Strengths are exact-once queue claims, disjoint spans, stale generation/incarnation quarantine, batching, and host mutation coverage. Open repair gates are the fixed 65,536-byte combined actor arena/accounting, P2-safe batch metadata reads, output overflow/exact-fit tests, and eventual actor-meshlet/production renderer cutover; target/Ymir/manual/FPS remain open. A repair is being prepared against the Task 14 ABI but must not be promoted until those gates pass.
Task 16 repair `d4efe0e9` is accepted for the infrastructure slice: independent rereview is SPEC PASS / QUALITY PASS, C0/I0/M0. The exact fixed arena is bank 24,088 + observer 12,320 + queue 5,644 + batches 1,024 + alignment 12 + outputs 22,448 = 65,536 bytes; 2,806 records fit, 2,807 fails, valid instance 64 is covered, output overflow quarantines the instance, and batch count uses a generation-checked P2 accessor. The broader task remains source-incomplete: actor-meshlet preparation, production drain/cutover, manifest drawable<=64 proof, target retirement-race, target/Ymir/manual/FPS, and final master merge are open.
Task 15 bounded VM commits `df95a107` and `52d45d5e` add a pointer-free sequence/layer interpreter slice with Project12x source timing/control-flow provenance, tempo accumulation, bounded loops/calls/returns, scalar note/control events, and malformed/truncated/unknown/non-progress/instruction-budget fail-closed tests. Serialized DLL-preflight `verify-sequence-vm` passes. Independent review is pending. The full 35-sequence/expanded seq00 differential gate is blocked by missing package bytes; no MC68000 image, SCSP transport, target, Ymir, manual, or audible claim is made.
Task 15 repair `211158ea` persists small-layer velocity/duration state from source initialization, adds explicit US versus EU/SH reserve-opcode formats and operand consumption, corrects EU/SH branch polarity, tracks channel active/finished and test-disabled state, and adds stack/loop/overflow/invalid-target/branch/layer regressions. Serialized DLL-preflight `verify-sequence-vm` remains green; independent rereview is pending. Full package, MC68000/SCSP, target, Ymir, manual, and audible gates remain blocked.
Independent rereview of `211158ea` is SPEC/QUALITY FAIL, C1/I2/M0: large-layer note1 emits stale duration, d7 replaces rather than updates selected channel-mask bits, and the combined EU/SH selector accepts unsupported/differing `0xda`, `0xf4`, and `0xdc` semantics. Small-layer persistence, reserve operand widths, branch polarity, channel state, and bounded-flow tests are confirmed. A targeted repair is active; Task 15 remains source-incomplete and no full package/audio claim is made.
Repair `b004fe7b` closes those bounded-slice findings: large-layer note0→note1 duration resets correctly, repeated d7 preserves previously active channel bits, EU/SH layer f4 uses bounded signed-relative flow, and EU/SH da/dc fail before incompatible operand consumption. Independent rereview is SPEC/QUALITY PASS, C0/I0/M0; `verify-sequence-vm` passes strict C11/Werror. Task 15 remains source-incomplete for full catalog/S64P/MC68000/SCSP/target/Ymir/manual evidence.
Task 17 bounded infrastructure commit `5b74081c` adds a 20-note protected-music/SFX allocator, integer desired voices with ADSR/release/tuning/pan/loop words, 32-slot minimal-diff shadow, rational timer/watchdog/generation claims, and the sole explicit `scsp_pcm8` MMIO boundary. Project12x/PoneSound/Yaul provenance is recorded; serialized gates `verify-audio-voice-allocator`, `verify-audio-slot-shadow`, `verify-audio-scsp-timer`, `verify-scsp-pcm8`, `verify-sequence-vm`, and `verify-audio68k-modules` pass, with empty m68k `nm -u`. Independent review is pending. Production heartbeat/MC68000 drain, full envelope/package data, target image, Ymir/tempo/manual/FPS remain open.
Independent rereview of `5b74081c` is SPEC/QUALITY FAIL, C1/I2/M0: stale-generation slot recovery can clear software state while hardware remains keyed, the freestanding m68k gate can false-green from cached objects without pinned tool discovery, and simplified software TL plus SCSP EG are both active while the report overstates source-faithful ADSR/release provenance. Allocator/timer/order/MMIO boundary gates pass. Repair is active; production heartbeat/MC68000 drain, full package, target, Ymir, tempo, manual, and FPS remain open.
Repair `144b4aa6` addresses the reviewed slice: stale-generation key-off is transactional with zero-capacity retry/telemetry, the m68k verifier discovers the pinned PoneSound/GCC toolchain, force-rebuilds and hashes artifacts, checks object format and empty `nm -u`, and software TL is the sole envelope owner with neutral SCSP EG words. Updated provenance records the full Project12x release-state range and explicitly calls the local linear envelope simplified. Independent rereview is pending; production heartbeat/package/target/Ymir/manual/FPS gates remain open.
Independent rereview of `144b4aa6` is SPEC/QUALITY PASS, C0/I0/M0 (audit `audits/audit-20260806-0155.md`). Real `scsp_pcm8` caller-path tests prove stale key-off success/retry semantics; the forced `-B` m68k gate discovers the pinned PoneSound/GCC toolchain and attests repository/compiler/fresh artifact hashes plus empty `nm -u`; software TL is the sole intentionally simplified envelope owner. Task 17 remains source-incomplete for production heartbeat/package/target/Ymir/manual/FPS integration.
Task 18 generic capability slice `a319583c` adds closure-derived RIGID/OPAQUE/STATIC_TRANSFORM/PLATFORM/COLLECTIBLE class bits, runtime TRANSFORM/SCALE/MATERIAL/SURFACE/LIFECYCLE masks, a 56-byte S64F family record, C unknown-bit/required-mask fail-closed validation, and closure-derived Python RED coverage. Serial actor-capability-bank, C mutation, closure Python 2/2, and generic actor-bank 4/4 gates pass. BOB reports 86 records/47 families, payload SHA `53f541e6…`, 13 unsupported, `complete_closure=false`; opaque and rigid/static queries fail closed with exact unresolved IDs. Independent review is pending; Task14 source registry/typed fields, Task16 production drain, target/Ymir/manual/FPS remain open.
Independent rereview of `a319583c` is SPEC/QUALITY FAIL, C0/I2/M1 (audit `audits/audit-20260806-0225.md`). The incompatible S64F 52→56-byte layout still uses version 1 and lacks a trusted external expected-hash boundary; the runtime-mask mutation fails at stale SHA before exercising unknown-bit validation; and “13 unsupported records” conflates 13 family representatives/reasons covering 14 closure records. Exact unresolved IDs and no-whitelist behavior are verified. Repair is active; Task14 typed fields, Task16 production drain, target/Ymir/manual/FPS remain open.
Second bounded-slice rereview confirms the ABI version/hash and terminology issues and adds: platform/collectible facts are granted by optional unowned `capability_hints` rather than generated schema-validated closure evidence, so real BOB has no authoritative such facts and hints could fabricate them; the safe state is unavailable/fail-closed until typed evidence exists. The diagnostic test also lacks an independent exact-family/UNKNOWN_CAPABILITY oracle. Task 18 remains active/source-incomplete; repair is pending.
Repair `929d100f` bumps S64F/FAMILY schema v1->v2 for 56-byte records, binds trusted external v2 header SHA `56e9a35c…` and stale-v1 SHA rejection, reseals the runtime-mask mutation before unknown-bit validation, adds independent oracle `bob_actor_capability_oracle_v1.json` for `bhv1Up[0x9322461f]`/UNKNOWN_CAPABILITY/PLATFORM/SURFACE, and marks capability hints analyzer-only/unverified. Serial actor-family/capability/queue, Python capability 3/3, and generic actor-bank 4/4 gates pass. Independent rereview is pending; typed platform/collectible/surface fields and production actor cutover remain open.
Evidence reconciliation `0b65d1d1` records serial `verify-actor-family-bank` PASS and exact counts: opaque 8 closure records/7 representatives (shared checkerboard `0xfc68327b`), rigid/static +5 representatives/+5 records, and non-rigid `bhvBreakBoxTriangle[0x08324fd7]`; total 13 unsupported representatives/reasons across 14 closure records. This closes the evidence-only residual; typed platform/collectible/surface fields, unresolved geo nodes, production actor cutover, target/Ymir/manual/FPS remain open.
Task 19 is active/source-incomplete. The implementer is extending the closure-derived actor capability inventory and immutable-snapshot articulated evaluator; the RED unsupported-record inventory and serial GREEN/review gates are not yet complete. No actor-capability completion, target, Ymir, manual, or FPS claim is made while the implementation is in flight.
Task 19 review `fcdf6245`: SPEC/QUALITY FAIL, C0/I4/M1. Fix round 1 dispatched to the original implementer. Findings: spawn provenance was incorrectly used as runtime PARENTED admission while Task 14 snapshots publish NO_PARENT; no pose/queue/lane/parent/held/model/despawn/stale-parent runtime contract was implemented; PARENTED IDs/report were inconsistent and not independently pinned; stored capability-mask mutation lacked a resealed hash test; and the target name/regression wave did not match the brief. Task 19 remains source-incomplete and unchecked.
Independent corroborating review of `fcdf6245` is also SPEC/QUALITY FAIL, C0/I6/M1: the commit is only fail-closed bank admission/diagnostics, with no articulated runtime/evaluator, dual-SH2 pose/parent/held/model/despawn/reward/stale proofs, stored-capability mutation or C/Python ABI parity coverage; the complete pose/queue regression wave timed out. Fix round 1 must address both review reports before rereview.
Design correction for fix round 1: because Task 14 still publishes `NO_PARENT` and lacks typed immutable parent/held/switch evidence, Task 19's accepted scope is narrowed to source-derived ANIMATED/SWITCH/MODEL_MUTATION admission, explicit unavailable/fail-closed PARENTED/HELD/LOD masks, exact independent BOB query oracles, and resealed stored-mask mutation coverage. The full articulated runtime/dual-lane proof is deferred to later actor cutover/closure; no synthetic host fixture may be promoted as production evidence.
Fix round 1 takeover: the original implementer was interrupted after partial scope narrowing; a fresh implementer is now assigned to finish the exact oracle, resealed stored-mask mutation, target-name/regression-wave alignment, and report/CHANGELOG narrowing under the corrected bounded brief. No runtime articulation claim is being added.
Task 19 fix round 1 `2222c375`: exact six-query oracle now pins counts, representatives, canonical record-set SHA-256s, unresolved IDs, unavailable dispositions, and behavior-child/boss-reward presence; C resealed +4 stored-mask mutation reaches unknown-capability validation; PARENTED/HELD/LOD selection fails closed; target is standardized to `verify-actor-capability-articulated`; prescribed serial make wave passes in 132.6s and standalone articulated CLI passes in 5.7s. Report/CHANGELOG narrow claims to admission-only; runtime pose/queue/lane/parent/held/model/despawn/reward/stale proof remains deferred. Scoped rereview pending.
Task 19 bounded slice complete (`2222c375`, scoped rereview SPEC/QUALITY PASS, C0/I0/M0). The broader runtime articulation/typed parent-held/dual-SH2/target/Ymir/manual/FPS task remains source-incomplete and deferred to later actor cutover/closure gates.
Task 20 is now active/source-incomplete against the bounded effect brief. Implementation owns source-derived effect inventory plus pointer-free descriptor/lowering helpers and fail-closed geo/capacity/mutation behavior; production observer capture, final BOB closure, target/Ymir/manual, and FPS remain open.
Task 22 preflight (baseline `dd8cd681`/`2222c375`) is complete as a read-only design/evidence lane; no code, build, target, Ymir, manual, or FPS claim was made. The report records the 86-record/47-family closure, missing unresolved-edge/capability totals, 13 unsupported reasons across 14 records, provisional 2,339,972-byte empty-dependency S64P, missing final linker/replay/launch tooling, and Mario-specific renderer cutover. Task 22 remains the sole final-link/reseal owner and implementation is not started.
Task 23 preflight (baseline `e0b946e8`/`1c89a2bb`) is complete as a read-only design/evidence lane; no code/build/target/Ymir/manual-audio/audible claim was made. The report records the missing final root and authoritative seq00/full AUDIO.DAT, weak sourceboot event/completion path, heartbeat-only production MC68000 image without Task15/17 linkage, missing PCM completion/identity status, exact semantic closure fields, and staged Waves 0–6 handoff.
Task 20 implementation handoff was restarted with a fresh implementer after the first lane produced no files; the bounded brief and preflight remain authoritative. No Task20 code or claim has landed yet.
Task 20 first implementation lane produced and host-validated the C descriptor/order module and RED tests but did not complete the oracle/Make/report handoff; it was interrupted and a fresh finalizer is now assigned. No Task20 commit or completion claim exists yet.
Task 20 bounded implementation `1c89a2bb` is complete pending independent review. Serial MSYS wave passes in 61.3s (`verify-actor-effects`, `verify-actor-batches`, `verify-actor-capability-bank`, `verify-actor-family-bank`, `verify-actor-instance-queue`); exact oracle is 5 BILLBOARD/30 ALPHA/18 TRANSLUCENT/34 SHADOW/0 PARTICLE/0 DECAL with 9 unsupported fail-closed records. Production observer capture, geo prerequisites, final closure, target/Ymir/manual/FPS remain open.
Task 20 review `1c89a2bb`: SPEC/QUALITY FAIL, C0/I2/M2. Fix round 1 dispatched. Findings: admission accepted zero actor-bank hash and dropped bank identity; order/lower bypassed shared unknown/reserved/source/lifetime validation; live plan still had superseded zero-unsupported/old gate wording; report did not itself name all exact class/role arrays. Production observer/geo/final closure/target/Ymir/manual/FPS remain open.
Task 20 preflight (baseline `fcdf6245`) is complete as a read-only design/evidence lane; no implementation or claim was made. The report records exact BOB effect counts (BILLBOARD 5, ALPHA 30, TRANSLUCENT 18, SHADOW 34, PARTICLE 0, DECAL 0), nine effect-related unsupported records, the source-authoritative graph/behavior/shadow seams, and reuse points for billboard math, actor meshlet depth admission, master batching, VDP1 lowering, and VDP2 composition. It identifies `GEO_CULLING_RADIUS`/`GEO_BRANCH_AND_LINK`, unresolved observer fields, heuristic effect roles, and cross-stream painter order as prerequisites. Acceptance is amended to source-complete versus target-complete; Task 20 implementation remains unstarted.
Task 21 preflight (baseline `b8f32fa0`) is complete as a read-only design/evidence lane; no implementation, build, target, Ymir, manual, or audible claim was made. The report records that feature-on sourceboot has no service/loader/transport/image link, the weak emitter drops events, the standalone soundtest owns the warned SNDOFF/SNDON path, Task 17 is not linked into the production 68K loop, real seq00/reproducible AUDIO.DAT are absent, and the provisional BOB S64P has empty audio dependencies. It defines the single CDFS/SNDOFF boundary, validated selected-chunk residency limits, six implementation/review waves, and Task 23 as the first target/audible proof after Task 22 reseal.
Task 21 Wave 0/1 bounded slice is source-complete pending review. RED recorded the missing wrapper and two warned soundtest convenience calls. The project-owned wrapper now owns generic SMPC commands, explicit 512-KiB/cold-recovery ordering, bounded stopped and READY-plus-heartbeat waits, typed completion/raw OREG31 telemetry, manifest-owner callbacks, and named faults. Final serial host evidence passes `verify-audio-sound-cpu-boot`, `verify-soundtest-boot`, `verify-pcm-protocol`, and `verify-audio-protocol-v2`; ownership scan is 2/2. Sourceboot service/package/residency/completion linkage, target/Ymir/manual/audible evidence remain open.
Task 21 custom-Yaul/MC68000 proposal audit is complete (read-only, no code/build/target claim). It maps the four requested directions to safe bounded slices: project-owned Yaul SMPC/sound-CPU wrapper; separate freestanding MC68000 target; pointer-free v2 completion/identity status; and deterministic asset/residency/build integration. It records exact Yaul/PoneSound provenance and confirms missing seq00/full AUDIO.DAT, final BOB S64P, strong sourceboot bridge, and production MC68000 linkage still block semantic/audible claims.
Task 20 fix round 1 `f161491a` is committed with canonical nonzero actor-bank hash admission, compact bank ID/token propagation through descriptor/lowering, one shared fail-closed descriptor validator for lower/order, and direct zero-hash/stale-token/crafted-field mutation coverage. Fresh serialized evidence passes: `verify-actor-effects` 57.5s and `verify-actor-batches verify-actor-capability-bank verify-actor-family-bank verify-actor-instance-queue` 58.8s. The plan’s individual Task20 test/GREEN steps are checked; scoped independent rereview is pending. Production observer capture, the two geo prerequisites, final closure, target/Ymir/manual/FPS remain open.
Task 20 fix round 1 rereview is SPEC/QUALITY FAIL, C0/I1/M1. Bank identity, shared validation, plan wording, and exact oracle binding pass. Remaining I1 is self-referential generation/package validation in the public lower API, which cannot reject an old descriptor; M1 is stale 64-byte/16-byte wording in the report. Fix round 2 is dispatched to repair the lower contract, add direct stale generation/package mutations, and correct all ABI-size evidence before rereview.
Task 20 fix round 2 `e61d3e8a` is committed. `sm64_saturn_actor_effect_lower()` now receives trusted current frame/package generations, and direct stale-frame/stale-package mutations fail closed; the report and brief consistently state the 68-byte descriptor/24-byte output ABI. Focused `verify-actor-effects` passes in 58.2s and the serialized actor regression wave passes in 59.3s. Scoped rereview is pending; production observer/geo/cross-stream/target/Ymir/manual/FPS gates remain open.
Task 20 bounded slice is accepted as source-complete after fix-round-2 rereview: SPEC/QUALITY PASS, C0/I0/M0. The individual plan tests and GREEN wave are checked. The broader production observer capture, GEO_CULLING_RADIUS/GEO_BRANCH_AND_LINK prerequisites, cross-stream merge, final closure, target/Ymir/manual, and FPS gates remain source-incomplete and unchecked.
Task 21 transition: preflight and custom-Yaul/MC68000 proposal audit are complete; implementation is now active for a bounded source/host boot-contract wave only. The first wave is limited to one project-owned SMPC/sound-CPU wrapper, cold-boot/recovery ordering, direct-call static gate, and fail-closed host tests. Real seq00/AUDIO.DAT/S64P, production completion/identity linkage, sourceboot feature-on, target/Ymir/manual, and audible evidence remain blocked and unchecked.
Task 21 Wave 0/1 bounded slice is source-complete pending review. RED recorded the missing wrapper and two warned soundtest convenience calls. The project-owned wrapper now owns generic SMPC commands, explicit 512-KiB/cold-recovery ordering, bounded stopped and READY-plus-heartbeat waits, typed completion/raw OREG31 telemetry, manifest-owner callbacks, and named faults. Final serial host evidence passes `verify-audio-sound-cpu-boot`, `verify-soundtest-boot`, `verify-pcm-protocol`, and `verify-audio-protocol-v2`; ownership scan is 2/2. Sourceboot service/package/residency/completion linkage, target/Ymir/manual/audible evidence remain open.
