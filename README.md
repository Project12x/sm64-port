# SM64 Saturn Port

This repository supports Sega Saturn only. For toolchain setup, builds, and
verification, see [the Saturn build guide](docs/saturn/BUILDING.md). Evidence
and runtime contracts live under `docs/saturn/`.

The sourceboot `diag-skip-geo` configuration is a default-off, invalid-state
upper-bound diagnostic; it is not a supported game mode.
