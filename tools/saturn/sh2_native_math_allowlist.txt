# Linked BOB replay route SH-2 native-math contract.
#
# The heat proof is function-level, never source-file-level.  The active
# SATURN_DEMO_PATH=1 / SATURN_SOURCEBOOT_ROUTE_REPLAY=1 route calls
# sm64_saturn_demo_render_frame() once per emitted frame (sourceboot/main.c),
# which calls demo_spatial_admit() and its ztreme frustum test with the default
# BSP-order configuration (saturn_demo_render.c:310).  The near-clip helper is
# intentionally absent: SATURN_DEMO_NEAR_CLIP defaults to 0 for this route.
#
# Every ROUTE_FUNCTION must have exact HOT rows for every linked native-math
# helper it calls. HOT_TOTAL is independently enforced, so removing a route
# function/category from this file makes the gate fail instead of reclassifying
# its call sites as COLD. A conversion must shrink the exact rows and total.
ROUTE_FUNCTION _sm64_saturn_demo_render_frame
ROUTE_FUNCTION _sm64_saturn_ztreme_frustum_aabb
HOT_TOTAL 16
HOT _sm64_saturn_demo_render_frame ___divdi3 5
HOT _sm64_saturn_demo_render_frame ___fixsfsi 7
HOT _sm64_saturn_ztreme_frustum_aabb ___divdi3 4
