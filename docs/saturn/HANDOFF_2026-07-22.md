# SM64 Saturn source-port handoff — 2026-07-22

## Read this first

This handoff supersedes
[`HANDOFF_2026-07-21.md`](HANDOFF_2026-07-21.md). That document's "Next
implementation: capture a real VDP1 source frame" section is now **done** —
the 2026-07-22 TIMELINE.md entry ("First visible original source geometry
through the VDP1 pipeline") recorded that gate at the emulator level. This
session raised the pipeline's per-frame triangle/command capacity on top of
that gate, using a strict before/after live-diagnostic bracket rather than
assumption. The honest result is **no visible change at the exact boot depth
tested** — read the "Outcome" section below before drawing any conclusion
from the capacity numbers alone. Everything else in the 2026-07-21 handoff
(source loop ownership, cart-loader status, known constraints, licensing
posture) is unchanged and still authoritative.

## Repository snapshot

| Item | Value |
| --- | --- |
| Repository | `Project12x/sm64-port` local clone in this workspace |
| Branch | `saturn/bootstrap` |
| Handoff commit | `0cab637 docs(saturn): Task 4 after-capture evidence, VDP1 arena capacity 512->2048` |
| Working-tree policy | No tracked modifications at handoff beyond this document and its own commit. |
| Short-term goal (now met, with a caveat) | Raise `SM64_SATURN_FAST3D_MAX_RESOLVED_TRIANGLES` (192 -> 1536) and `SOURCEBOOT_VDP1_COMMAND_CAPACITY` (512 -> 2048) and verify the effect with a real live before/after capture. Both constants are raised and the host/cross-compile regressions pass, but the capture proved the raise produced **zero visible or counter-level change** at the tested boot depth — see below. |
| Next short-term goal | Root-cause why the resolve stage rejects 554 of the 573 transformed triangles at this boot depth (`rej_near_far=447`, `rej_backface=107`), with 18 emitted and one triangle's terminal bucket not captured by this snapshot (554+18=572, one short of 573 — see the "Outcome" section's caveat), before any further capacity work — see "Next implementation." |

## What shipped this session

### Two capacity constants were raised, grounded in real measured data

- `src/port/saturn/gfx/saturn_fast3d_frontend.h`:
  `SM64_SATURN_FAST3D_MAX_RESOLVED_TRIANGLES` **192U -> 1536U** (HWRAM,
  per-frame resolved-triangle buffer). `SM64_SATURN_FAST3D_MAX_VERTICES`
  stays at `64U` — it is a per-`G_VTX`-command scratch buffer, not the
  per-frame accumulator, and was not implicated in the measured bottleneck.
- `src/port/saturn/sourceboot/main.c`:
  `SOURCEBOOT_VDP1_COMMAND_CAPACITY` **512U -> 2048U** (LWRAM, VDP1 command
  arena) — raised to track the resolve-stage cap 1:1, since
  `saturn_fast3d_vdp1_emit.c` emits exactly one `vdp1_cmdt_t` per resolved
  triangle.

The real numbers grounding `1536` (from the updated budget comment added
directly above `typedef struct sm64_saturn_fast3d_frontend` in
`saturn_fast3d_frontend.h`):

> UPDATE (2026-07-22, capacity increase): the ~380-byte-free HWRAM figure
> [from the 2026-07-21 handoff] is now stale. Earlier the same day, SM64's
> main pool (previously 0x30000-0x60000 bytes of HWRAM `.bss`) moved to
> LWRAM (`src/port/saturn/sourceboot/main.c`) to fix a boot-fatal heap
> collision — that freed ~191 KiB of HWRAM (`___end` measured at
> `0x060D0284` vs. the `ram` region's top `0x06100000`). This struct's
> `resolved[]` array was grown from 192 to 1536 entries (+21,504 bytes) on
> the strength of that headroom: 1,536 is grounded in this project's own
> captured real Bob-omb Battlefield frame data (1,365-1,431
> triangles/frame, `saturn_fast3d_frontend.c`'s NEAR/FAR depth comment),
> stays comfortably under Sega's own SGL 3.02j default of 1,786
> polygons/frame (`docs/saturn/SGL_REFERENCE_NOTES.md`), and is far inside
> this project's own castleviewer precedent (a working scene with
> 1,032-1,105 live VDP1 commands, `RENDERER_PRIOR_ART.md`).

And for `2048` (from the matching comment in `main.c`):

> Capacity raised 512 -> 2048 (2026-07-22) to track
> `SM64_SATURN_FAST3D_MAX_RESOLVED_TRIANGLES`'s 192 -> 1536 increase 1:1
> (`saturn_fast3d_vdp1_emit.c` emits exactly one `vdp1_cmdt_t` per resolved
> triangle). 2048 * 32 bytes = 64 KiB, trivial against the ~1 MiB free in
> the `lwram` region (`sourceboot-cart.x`) — no linker script change
> needed.

### Verified state

- A new host-native test (`test_frontend_resolved_capacity_exceeds_old_192_cap`
  in `tools/saturn/runtime_contract_test.c`) resolves 200 independent valid
  triangles in one frame and asserts none are capacity-rejected. It failed
  against the old 192 cap as expected, then passed after the constant was
  raised.
- `make -f Makefile.saturn.mk verify-runtime-contracts` passes (all
  contract tests, including the new one).
- Full `sourceboot` cross-compile (forced clean rebuild — the mangled
  `@`-path object files and header-only-change staleness both apply here,
  per the 2026-07-21 handoff's build-system traps) and `make verify` both
  pass: entry point `0x6004000`, `.cart_rodata` at `0x22400000`, cart image
  within the 4 MiB budget.
- HWRAM headroom was re-measured directly with `nm` after the rebuild
  rather than assumed from arithmetic, per the project's build-preservation
  discipline.

### Before/after live-diagnostic bracket

Both captures used the identical deterministic recipe: 240 BIOS frames + 3,600
post-poke frames (the corrected depth — see the CPU-hang section below), same
`ymir-headless`/IPL/cue paths, same freshly re-resolved profile-struct probe
address (`0x060b9368`, re-resolved via `nm` both times per the project's
"never reuse an address from an earlier message" rule).

| | Before (`e2-sourceboot-capacity-before-2026-07-22`) | After (`e2-sourceboot-capacity-after-2026-07-22`) |
| --- | --- | --- |
| Screenshot | [before](evidence/screenshots/e2-sourceboot-capacity-before-2026-07-22.png) | [after](evidence/screenshots/e2-sourceboot-capacity-after-2026-07-22.png) |
| Report | [before](evidence/reports/e2-sourceboot-capacity-before-2026-07-22.json) | [after](evidence/reports/e2-sourceboot-capacity-after-2026-07-22.json) |
| `frame_serial` (sanity gate) | 47 | 47 |
| `frame_hash` | `10224221a6b5d7bc7d02b44799cf877a` | `10224221a6b5d7bc7d02b44799cf877a` |
| PNG SHA-256 | `3d4e2da7...c8be91c7e5a` | `3d4e2da7...c8be91c7e5a` (byte-identical file) |
| `triangle_cnt` (decoded) | 574 | 574 |
| `tri_transformed` | 573 | 573 |
| `tri_emitted` (resolved) | 18 | 18 |
| `tri_vdp1_emitted` | 0 | 0 |
| `rej_cmd_cap` | 0 | 0 |
| `rej_near_far` / `rej_backface` / `rej_degenerate` | 447 / 107 / 0 | 447 / 107 / 0 |
| attribution: `w_nonpos` / `z_near` / `z_far` / `offscreen` / `span` | 293 / 0 / 154 / 0 / 0 | 293 / 0 / 154 / 0 / 0 |

The sanity gate (`frame_serial` must be well under the tens-of-thousands
range that flagged the earlier stale-probe misread this session) passed for
both captures at 47, confirming both reads are live, not stale/misaligned.
Both screenshots were independently re-read with the Read tool for this
handoff: both are solid black 320x224 frames, with no visible geometry at
all, consistent with `tri_vdp1_emitted=0` in both captures.

## Outcome: no visible or counter-level change at this boot depth

Per the plan's Task 5 Step 1 outcome (b): **no TIMELINE.md success entry was
written.** The capacity increase is real, tested, and correct at the
capacity-rejection level (`rej_cmd_cap=0` and no VDP1-arena rejects in either
capture — the resolve buffer and command arena were never what filtered
triangles out), but it produced zero effect at frame 47 of this boot,
because raising a downstream capacity ceiling cannot change anything when
the actual constraint is upstream of it. Out of 573 transformed triangles at
this frame:

- **447 are rejected by `rej_near_far`**, and of those, 293 fail the strict
  `w <= 0.0f` reject (`profile->reject_w_nonpositive`, clip-space w
  non-positive — behind the camera or a degenerate perspective divide) and
  154 fail `z_far` (beyond `SM64_SATURN_FAR_DEPTH = 16384`).
- **107 are rejected by `rej_backface`.**
- **0 are rejected by `rej_degenerate`, `rej_vtx_range`, or `rej_cmd_cap`.**
- **18 survive** all filters into `tri_emitted` — matching this
  project's independently-documented "~18/frame" figure — and **none of
  those 18 reach `tri_vdp1_emitted`** in either capture (it is 0 both
  before and after), which is the direct, verified reason the two frames
  are pixel-identical: nothing was submitted to VDP1 in this specific frame
  regardless of either capacity ceiling.

**Caveat: the six terminal buckets above sum to 572, one short of the 573
`triangles_transformed` total** (447 + 107 + 0 + 0 + 0 + 18 = 572, decoded
directly from both JSON reports' `probe_window` bytes against the real
`sm64_saturn_fast3d_profile_t` layout, not just this document's
transcription). `sm64_saturn_fast3d_resolve_triangle()`
(`src/port/saturn/gfx/saturn_fast3d_frontend.c:155-412`) increments
`triangles_transformed` exactly once at entry (line 204, the sole increment
site) and every code path terminates through exactly one of six
mutually-exclusive counters (`reject_vertex_range`, `reject_near_far`,
`reject_backface`, `reject_degenerate`, `reject_command_capacity`,
`triangles_emitted`), so by construction the six should sum to the total —
they do not, by exactly one triangle, in both the before and after capture
(which are otherwise byte-identical, including `frame_serial=47`). The most
likely explanation is a probe read landing mid-frame: the 573rd triangle's
`triangles_transformed++` had already been recorded at the instant
`mem.peek` sampled live SH-2 RAM, but that same triangle's terminal
reject/emit counter had not yet been written — the same class of
read-timing artifact this project's own `frame_serial` sanity-gate
methodology exists to catch, just a 1-triangle-scale instance of it rather
than the multi-order-of-magnitude misread that gate was built for. This
does not change any conclusion above (the 447/107/18 figures and the
`tri_vdp1_emitted=0` result are unaffected), but the 573 total should be
read as accounted for to within one triangle, not fully closed.

This is not a capacity problem. Raising `SM64_SATURN_FAST3D_MAX_RESOLVED_TRIANGLES`
and `SOURCEBOOT_VDP1_COMMAND_CAPACITY` was necessary infrastructure (real
Bob-omb frames run 1,365-1,431 triangles, and 192/512 would have capped hard
against that once the true bottleneck below is fixed), but at frame 47 the
resolve stage never comes close to either the old or new ceiling — it discards
554 of 573 triangles before capacity is ever checked.

## Next implementation: root-cause the near/far and backface reject dominance

The concrete next diagnostic target, in priority order:

1. **`w_nonpos` (293 of 573, the single largest bucket).** A triangle with
   `w <= 0.0f` at this frame is either genuinely behind the camera (early in
   a fly-in/setup frame this is plausible) or the projection/view matrix
   feeding this frame is wrong in a way that produces bogus w values for
   otherwise-valid geometry. Compare the live modelview/projection matrix
   state at frame 47 against the source SM64 camera state at the same real
   game frame to tell those two cases apart before touching any renderer
   code.
2. **`z_far` (154 of 573).** Confirm whether these are genuinely
   far-clipped scenery (distant mountains/sky per the existing
   `SM64_SATURN_FAR_DEPTH=16384` calibration note) or another camera/matrix
   symptom of the same root cause as (1).
3. **`rej_backface` (107 of 573).** Lower priority — backface culling
   rejecting roughly a fifth of geometry is architecturally expected for a
   closed scene; only worth revisiting after (1) and (2) are understood.
4. Only once the resolve-stage attribution is understood and, if needed,
   fixed, should the capacity increase be re-evaluated at this same frame
   depth (and ideally at a few other frame depths/camera angles) to see
   whether it then produces the expected larger, recognizable terrain
   silhouette.

This session's capacity increase should be kept — it is real, tested,
correctly documented infrastructure for whenever the actual bottleneck is
fixed — but no visual-gate or TIMELINE.md claim should be made from it until
a capture at a boot depth/camera angle where triangles actually reach
`tri_vdp1_emitted` shows a measurable difference.

## Critical: Boo-behavior CPU hang discovered during this effort

Investigating an earlier implausible diagnostic read (`frame_serial` in the
tens of millions for a run that can only have executed a few hundred real
game frames) found a genuine SH-2 CPU hang, not a tooling bug:

- **A full SH-2 register freeze occurs somewhere between `--post-poke-frames
  3600` and `3800`** on this exact boot (240 BIOS frames, USA BIOS input
  macro, `dram_cart`, the `e2-bob` sourceboot target). Every general-purpose
  register, `PC`, `PR`, `SR`, `GBR`, `VBR`, `MACH`, and `MACL` were observed
  bit-for-bit identical across a 3,200-emulated-frame span once the hang
  begins — this is a true CPU freeze, not merely a static scene.
- **`PC` is stuck inside `_boo_update_during_death`, in the middle of
  evaluating `_cur_obj_has_behavior(&_bhvBoo)`.**
- Cache-coherency, stale-probe-address, and struct-layout theories were all
  tested and ruled out as explanations for the implausible early reading —
  it is a real, reproducible hang in the emulated program, confirmed by
  deliberately capturing at `3600` (plausible, live, `frame_serial=47`) and
  `14000` (frozen) and finding the register state at `14000` matches a
  region deep inside the freeze window.
- **This casts real doubt on the existing 2026-07-22 "First visible
  original source geometry" TIMELINE.md milestone entry**, which was
  captured at `--post-poke-frames 14000` — past the start of this hang
  window. That capture's frame may not be live gameplay evidence at all; it
  needs **separate re-verification** (a fresh capture across a range of
  post-poke depths bracketing the hang, confirming which depths are live
  and which are frozen, and re-examining what that milestone's screenshot
  actually shows) before it can be trusted as anything more than "the
  emulator produced this specific frame of a specific bit-pattern in VDP1
  VRAM at that address at that instant" — the milestone's underlying visual
  gate claim (untextured/flat-shaded source geometry reached VDP1 at some
  point) is very likely still true given this session's own live 3600-depth
  data shows a real, if currently non-rendering, resolve pipeline, but the
  *specific screenshot* backing that milestone needs re-confirmation against
  a known-live depth.
- **Root-causing the hang itself was explicitly out of scope for this
  plan.** This plan only needed one confirmed-live capture depth (3,600) to
  bracket the capacity-increase before/after comparison, and got one. The
  hang is a separate, higher-priority open problem: it affects every future
  diagnostic capture on this boot (any depth past ~3,600 risks sampling a
  frozen CPU rather than live gameplay) and, if it reproduces on real
  hardware rather than being an emulator-specific artifact, it would be a
  release blocker for anything involving Boo enemies. This is recorded here
  as open, higher-priority work — not resolved by this session.

## Known limitations (carried forward, still accurate)

Everything in the 2026-07-21 handoff's "Known limitations" section (no
texturing/Gouraud, no polygon clipper, non-full-height viewports unhandled,
uncoordinated cross-binary LWRAM allocation, `matrix_stack.depth` not
resetting per frame) remains unchanged and accurate. This session did not
touch any of that surface.

## Follow-up outside this session's scope

- **The Boo CPU hang** (see above) — root cause unknown, needs its own
  investigation.
- **Re-verification of the 2026-07-22 "first visible geometry" milestone
  screenshot** (`e2-sourceboot-first-geometry-2026-07-22.png`, captured at
  `--post-poke-frames 14000`) against a confirmed-live depth.
- Everything carried forward, unresolved, from the 2026-07-21 handoff's own
  "Follow-up outside this session's scope" section (the stale design-spec
  matrix-format claim, Whomp's Fortress not linked into `sourceboot`, and
  the POSIX-path bug in `verify-runtime-contracts`'s Makefile recipe,
  `task_5e83f53b`) remains open and untouched by this session.
