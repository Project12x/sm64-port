# Task 7 absolute-rate capture — 2026-07-28

Chosen degradation setting: dual worker, `SATURN_DEMO_VIEW_RADIUS=2048`,
`SATURN_DEMO_POLY_TIER=0`. The same ELF/CUE lineage was captured twice with
the established BIOS handoff-yield recipe and `--dram-cart`:

| capture | Ymir screenshot sequence | `frame_serial` | route endpoint |
| --- | ---: | ---: | --- |
| 1,200-frame window | 3,900 | 118 | replay tick 118 (warm-up window) |
| 3,600-frame window | 8,700 | 803 | replay tick 600 |

The equal-lineage differencing window is 4,800 emulator VBlank frames and 685
completed submits, giving an absolute emulator rate of
`685 / 4,800 * 60 = 8.5625 FPS`. This is above the plan's ≥5 FPS visible
milestone but below the 15 FPS median / 12 FPS 1%-low exit gate. The current
phase fields do not provide a percentile distribution, so a 1%-low number is
not claimed. The long-window profile reports `fault_flags=0`,
`slave_timeouts=0`, route `replay_ticks=600`, and the same checkpoint hash as
the serial A/B oracle.

This is emulator-derived absolute-rate evidence, not a retail-hardware claim.
The long-window screenshot is retained as the paired visual evidence from
`task5b-cachepart-dual-2026-07-28`; owner visual confirmation is still needed
before gallery promotion.
