# Saturn port handoff — source-state/culling checkpoint

Date: 2026-07-19
Branch: `saturn/bootstrap`
Last committed policy checkpoint: `67376e5 docs: allow compatible GPL source adaptation`

## Current continuation checkpoint

The working tree now contains the verified input/cache slice that should be
committed intentionally after review. The Yaul controller adapter follows the
upstream SMPC `pressed.raw` normalization (active-high) and only consumes
analog axes from an actual `ID_ANALOG` report. A 900-frame neutral probe reads
all-zero `OSContPad` button/stick state, so the previous idle wandering/all-
buttons failure is no longer the baseline.

The Castle viewer also exposes `saturn_frame_sample`, a new 42-byte,
sequence-guarded all-16-bit record written after VBlank. At the current neutral
Ymir software-VDP boundary it reports 2,296 update, 5,041 sort, 569 command,
2,634 wait, and 3,495 VBlank FRT ticks: 10,540 render / 14,035 loop ticks,
or 14.93 loop FPS. This is emulator evidence only. The persistent command
lowering cache is pixel-identical to its pre-cache frame at the neutral state;
the VDP1 list is still re-armed/uploaded every frame.

Evidence:

- [stable neutral screenshot](evidence/screenshots/ymir-m4-stable-frame-sample-neutral-2026-07-19.png)
- [stable neutral report](evidence/reports/ymir-m4-stable-frame-sample-neutral-2026-07-19.json)
- [stable right-input screenshot](evidence/screenshots/ymir-m4-stable-frame-sample-right-held-2026-07-19.png)
- [stable right-input report](evidence/reports/ymir-m4-stable-frame-sample-right-held-2026-07-19.json)

The post-run Ymir pulse currently ends in a neutral sample, so it is not yet
accepted as a movement-position proof; use a pulse-time probe or longer-lived
input injection before claiming automated locomotion. Bob-omb Battlefield and
the global coarse hierarchy remain open.

## Objective still open

The production objective remains unchanged: use the real SM64 source path,
make one global painter/occlusion policy, reach the 15–20 FPS target on the
4 MiB RAM-cart Saturn profile, and then generalize the same compiler/package/
renderer to Bob-omb Battlefield. Do not create a Bob-specific viewer or paper
over ordering with scene-local passes.

## Proven in this checkpoint

- `tools/saturn/extract_mario_actor.py` now threads Fast3D light, texture,
  combiner, and `G_CULL_BACK` state through source-ordered GeoLayout parts.
- The regenerated Mario intake is 788 source triangles, 644 compiled
  primitives, 23 originating display-list clusters, and 521 cluster-local
  vertex references. The report records persistent source state and 644
  source backface-cull primitives.
- Source material audit: the M-logo backing and shared forearms inherit red;
  facial detail inherits beige; buttons remain blue.
- `saturn_rgb1555()` and the UV bake canonicalize N64 A1=0 to exact VDP1
  `0x0000`. The regenerated 16x16 bank contains 7,216 transparent words,
  5,584 opaque MSB-set words, zero nonzero/MSB-clear words, and zero `0x7FFF`.
- The Castle runtime uses one shared view-space triangle-facing helper for
  Castle and Mario. Mario's generated cull metadata is applied before actor
  bucket insertion, with the same near-clip exception as static geometry.
- Host validation is green: 57 tests in `tools/saturn/test_tools.py`.
- The Yaul Castle image builds successfully with the pinned cross-toolchain.

## Captures and measurements

The source-cull capture is preserved at:

- Screenshot: `docs/saturn/evidence/screenshots/ymir-m4-source-state-backface-cull-2026-07-19.png`
- Report: `docs/saturn/evidence/reports/ymir-m4-source-state-backface-cull-2026-07-19.json`

The probe at the rebuilt `_frame_profile` address decodes as:

| update | sort | command | wait | vblank | render ticks | loop ticks | render FPS | loop FPS |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 4045 | 9902 | 701 | 2912 | 3495 | 17560 | 21055 | 11.93 | 9.95 |

This is a real Ymir software-VDP measurement, not a claim of retail hardware
performance. The fixed telemetry address used by `capture_hwtest.py` is not
the Castle viewer's frame-profile block; decode the paused probe as the
big-endian layout `>5H2x2I`.

## Experimental cache currently in the worktree

`src/port/saturn/castleviewer/main.c` contains a camera-keyed static painter
token stream and skips the source camera/floor query when Mario's world
position is unchanged. This preserves the BSP's far/near traversal and leaf
insertion order. It is **not accepted as a performance win yet**:

- Screenshot: `docs/saturn/evidence/screenshots/ymir-m4-static-stream-cache-source-cull-2026-07-19.png`
- Report: `docs/saturn/evidence/reports/ymir-m4-static-stream-cache-source-cull-2026-07-19.json`
- Measurement: 11.93 render FPS / 9.95 loop FPS (4063 update, 10018 sort,
  695 command, 2762 wait, 3494 vblank in the captured frame).

The next agent must instrument cache-hit/miss and split `sort_scene()` into
Mario classification, static-stream rebuild, and stream emission timings.
Do not claim the cache works until a deterministic idle capture shows a
material sort/update reduction and an image hash equal to the uncached path.

## Immediate next actions

1. Capture a deterministic pulse-time input sample (including button/stick and
   world position) so movement is proven while the input is held, not only
   after the pulse has expired.
2. Capture equal-frame uncached/cached idle runs with identical BIOS input and
   compare screenshot hash, queue count, cull/reject counts, and the new
   sequence-guarded frame sample.
3. If the cache is valid but sort remains dominant, adapt the bounded
   Z-Treme-style coarse hierarchy/frustum state before exact BSP traversal;
   keep exact BSP ordering inside visible leaves.
4. Use SlaveDriver's bounded dependency/clip records as the next shared
   renderer adaptation candidate, retaining GPL provenance and notices.
5. Only after the Castle image-equivalence gate and measurable budget win,
   generalize the area package root/texture manifest/IA16 decode to Bob-omb
   Battlefield. Bob's current audit is in the roadmap/ledger and reports
   1,101 source triangles, 1,016 primitives, and a need for bounded coarse
   visibility before exact BSP.

## Build and capture commands

```text
C:\msys64\usr\bin\bash.exe -lc "cd /c/Users/estee/Documents/Codex/2026-07-16/i-want-to-postulate-a-port/sm64-port && source .yaul.env && make -C src/port/saturn/castleviewer"

.venv-saturn-tools\Scripts\python.exe tools\saturn\test_tools.py
```

The user BIOS and ROM remain outside Git. Do not stage `build/`, BIOS files,
ROM-derived headers, or the broad historical evidence backlog with `git add .`.
