# Peer Audit — 2026-08-13 18:40

## Context

- Prior agent: multiple Codex agents.
- Task claimed: make a normal, generic BOB actor path converge to a visually acceptable, manual-testable Saturn demo, with texture/material support and later real audio.
- Files examined: `docs/superpowers/plans/2026-08-12-saturn-actor-bank-v2-textures.md`, `Makefile.saturn.mk`, `src/port/saturn/sourceboot/Makefile`, `src/port/saturn/sourceboot/source_audio_stub.c`, `src/port/saturn/sourceboot/source_audio_semantics.c`, the current sourceboot map files, current generic-BOB replay profile/build logs, and the uncommitted painter-chain changes.
- Commands run: scoped `git status`, two-week `git log`/history counts, `git diff --shortstat`, `make -f Makefile.saturn.mk verify-vdp1-painter-chain`, `python tools/saturn/test_render_snapshot_source.py`, and map/log searches for the HWRAM assertion.

## Findings

### Critical

1. **The current visual repair cannot produce a target artifact.** The host-only painter-chain test passes, but the exact target map for the new identity ends at `0x060fe228`; the required TLSF heap floor is `0x1f00`, so the linker rejects it. The last linked artifact ended at `0x060fe068`, leaving `0x1f98`; the change consumed 448 bytes, leaving only `0x1dd8`—296 bytes short of the required floor. See `build/saturn/sourceboot/e2-bob-identity-id-93963fa46432d708/obj/sm64-saturn-sourceboot-e2.map:41232-41235` and the previous artifact's `...id-d970fb4b1ae7bc4d...map:41233-41236`. Therefore this branch has **no new Ymir/manual candidate**. The heap assertion did the right thing; lowering it would conceal memory debt.

### Important

1. **The normal replay is still intentionally silent.** Its profile selects `audio-stub-v1` and has `features.semantic_audio: 0` in `.tmp-normal-bob-replay-profile.json:1`; the build wrapper exports `SATURN_FEATURE_SEMANTIC_AUDIO=0` in `.tmp-normal-bob-replay-build.sh:20`. The source Makefile then selects `source_audio_stub.c` at `src/port/saturn/sourceboot/Makefile:465-470`, whose public sound and music calls are no-ops (`source_audio_stub.c:15-48`). Even the optional semantic adapter has weak default emit/poll hooks that accept events without a real transport (`source_audio_semantics.c:44-68`). Current code cannot meet a "music plus triggered SFX" demo claim.

2. **The last two weeks show a format-and-review-first loop, not a live-slice-first loop.** From 2026-07-30 through the current `e399b1c1`, the branch contains 885 commits: 355 `docs(...)`, 236 `fix(...)`, 87 `feat(...)`, and only 10 `perf(...)`; the diff from the preceding commit changes 664 files with 235,076 insertions. The active plan marks Tasks 1–8 complete before Task 9's first normally spawned Bob-omb consumer (`...actor-bank-v2-textures.md:18-46`). This is the same sequencing the repository rule now forbids: normal target observation was deferred behind parser, bundle, provenance, residency, publication, and review work.

3. **The current package is not full BOB actor coverage.** The plan accurately says it packs the exact 14 measured direct-textured BOB keys (`...actor-bank-v2-textures.md:27-29`, `81-88`), while the desired normal-level gate calls for multiple representative actors (`63-69`). That is a valid incremental subset, but it is not evidence that all normal BOB actors now render. Broaden only after one recognisable normal Bob-omb is visually accepted through the exact same path.

4. **The worktree is not a clean decision point.** Scoped `git status` reports 89 tracked modifications and 242 untracked paths. Much is pre-existing concurrent work and evidence, but it means a build, screenshot, or manual observation from this worktree cannot by itself establish a reproducible baseline unless the exact inputs are recorded. This is especially risky while target maps and untracked replay scripts are being used to decide the next architectural step.

### Minor

None.

### Discrepancies between summary and code

1. **A generic Bob-omb route exists, but a generic visual gate does not.** The plan now names `bhvBobomb` as the first witness (`...actor-bank-v2-textures.md:109-111`), and the host painter-chain test passes. However the current visual output has not passed user inspection, and the attempted target repair does not link. Calling it a demo or an accepted on-screen actor would overstate the evidence.

2. **"Semantic audio" is not real Saturn audio transport.** It is a useful policy adapter, but its default backend is intentionally empty; the selected profile does not even enable it. It must not be counted as music/SFX integration.

3. **The current HWRAM failure is evidence of the missing per-change target budget gate.** The plan contains a strong memory table (`...actor-bank-v2-textures.md:79-92`), but a renderer-only ordering change was able to reach the final link 296 bytes over the heap floor. The map delta should have been a pre-merge/live-slice gate, not the last step before a manual capture.

## What was done well

- The linker refused the unsafe HWRAM layout instead of allowing libyaul's heap to mirror into low memory.
- The new host painter-chain test proves stable far-to-near linking and atomic failure for an invalid depth tag; it is a focused regression test, not empty test inflation.
- The plan correctly replaced the earlier injected Cannon witness with a normally spawned `bhvBobomb`, and correctly prohibits new wire formats before that generic BOB gate.
- The repository has increasingly explicit ownership/margin records. Those are useful only if they become launch gates rather than post-hoc review evidence.

## Recommended next actions

1. **Freeze architecture, formats, package broadening, release work, and new audit campaigns.** Preserve the last linked interactive baseline separately; do not reset or overwrite the current dirty worktree.
2. **Treat the 296-byte HWRAM deficit as the sole active blocker.** Recover it without reducing the TLSF floor, command capacity, actor capacity, or other live budget. The owner-authorized fixed Mario Gouraud table is a plausible bounded code-size tradeoff, but it needs a small focused test and a fresh target map proving the recovered margin.
3. **Immediately link and launch the exact normal BOB replay after that single repair.** The acceptance capture must show Mario at normal scale with stable ordering/Gouraud and a normally spawned, recognisable textured Bob-omb. Wait for the level to render before taking the frame; no injected Cannon or forced object may substitute.
4. **Then connect the existing PCM/68K path to the normal BOB profile and prove one background track plus one game-triggered SFX in the same run.** Do not call the semantic adapter or soundtest a demo substitute.
5. **Adopt a live-slice rule for the remainder of the week:** one active blocker, one target build/map check after every memory-affecting behavior change, one Ymir/manual observation as soon as it links, and no task may be called complete from host tests alone. If a repair fails or expands scope, return to the last linked baseline and make the smallest incremental correction there.

## Live follow-up — 2026-08-13

The initial HWRAM finding was repaired without reducing the TLSF floor or any
actor/VDP1 capacity. The final issue was stale `JUMP_ASSIGN` type bits in
reused VDP1 frame-bank commands: lowerers replace the raw depth-bin tag but do
not need to replace the link type, because `vdp1_cmdt_jump_assign()` does that
when the final bin chain is written. The finalizer now validates all raw tags,
clears only prior link-type bits, and then relinks the existing command bank.

The exact normal BOB candidate `id-d236c3da23f7144a` links with
`___end=0x060fe0e8` (7,960 B versus the 7,936-B required TLSF margin); its
manifest verifies as
`13ae0f0f0df03eb0ace66051820aca02be0f8329e34926efba78deb94a26375a`.
The identity-bound Ymir route reached frame 14,220 / presentation generation
278, selected five ordinary actor instances, observed one normal
`bhvBobomb` / `MODEL_BLACK_BOBOMB` as `render_active`, and completed 69 generic
outputs with 27 actor Gouraud tables and no failed render generation.

This is a **live generic-path proof only**. The captured image remains subject
to owner visual acceptance for Mario scale/Gouraud/order and recognisable
Bob-omb material. Audio still selects `audio-stub-v1`, so this is not a
music/SFX demo. No actor expansion, new wire format, release, or unrelated
architecture work is authorized by this result.
