# Peer Audit — 2026-08-13 05:52 -04:00

## Context

- Prior agent: Codex / multi-agent implementation history.
- Task claimed: establish a normal, generic Bob-omb path on the Saturn target and progress toward a runnable BOB demo.
- Files examined: `src/port/saturn/sourceboot/main.c`, `src/port/saturn/sourceboot/source_scene_bundle.c`, `src/port/saturn/gfx/saturn_vdp1_frame_bank.c`, `tools/saturn/source_scene_bundle_test.c`, `src/port/saturn/sourceboot/Makefile`, `src/port/saturn/sourceboot/source_audio_stub.c`, `.tmp-normal-bob-replay-profile.json`, `.tmp-normal-bob-replay.json`, and the current candidate manifest.
- Commands run:
  - `git status --short` and `git log --oneline -12`
  - `C:/msys64/usr/bin/make.exe -f Makefile.saturn.mk verify-source-scene-bundle verify-vdp1-painter-chain`
  - `release_manifest.py verify --required-profile sourceboot-bob-normal-replay-local`
  - targeted JSON/profile/source inspection.

## Findings

### Critical

None.

### Important

1. **The current Bob-omb witness is a development liveness proof, not a demo or release candidate.**  The direct manifest verifier accepts `build/saturn/sourceboot/e2-bob-identity-id-d236c3da23f7144a/saturn-release-manifest-v1.json` (manifest SHA-256 `13ae0f0f...a26375a`), but the manifest itself declares `provenance.closure_clean: false` and `reproducibility: "uncompared"`.  Its Git revision is `e399b1c1`, while the target-affecting source is still uncommitted in the worktree.  It is valid development evidence, not a releasable/manual-acceptance artifact.

2. **The narrow Task 8 command-bank return repair exists in `main.c`, but its exact integration ordering lacks a direct regression.**  The current source correctly performs `sm64_saturn_vdp1_frame_bank_set_init()` first, performs the cold actor upload, then restores both VDP1 command-bank prefixes with `sm64_saturn_vdp1_backend_init_with_storage()` at `src/port/saturn/sourceboot/main.c:1893-1911`.  This addresses the prior concern that the 2,560-byte cold upload overwrites bank 0 after both frame banks are registered.  However, the passing `verify-source-scene-bundle` host binary compiles `source_scene_bundle.c`, not `main.c`, and its Make invocation generated/used `scene-v3-g15`; the live candidate is package generation 22.  A tiny source/host ordering test (or target-equivalent fixture) must prove the cold-stage overwrite is followed by the prefix restoration before first `begin_build`.

3. **The generic-path proof is real but does not establish visual correctness.**  The current identity-bound capture reports a normal observer record with `family_id: 7`, `model_id: 188`, `active: 1`, and `render_active: 1` at source generation 279; it also reports 69 generic output records and 27 generic Gouraud tables with no failed render generation.  That disproves the old injected-Cannon-only claim.  It does not check pixels, texture sampling, fixed Mario Gouraud appearance, scale, or painter ordering.  The existing visual defects reported in manual testing therefore remain open rather than contradicted by this telemetry.

4. **Audio is still deliberately absent on the only current normal-Bob target.**  `.tmp-normal-bob-replay-profile.json` fixes `features.semantic_audio` to `0` and selects `audio-stub-v1`.  `src/port/saturn/sourceboot/Makefile:465-470` consequently includes `source_audio_stub.c`; that file’s `play_sound`, `audio_signal_game_loop_tick`, `sound_init`, and `audio_init` are no-ops.  Neither music nor game-triggered SFX can be claimed for this candidate.

5. **The current bundle is a narrow BOB subset, not complete normal-BOB actor coverage.**  The live scene probe reports 47 families and 15 packed variants.  The plan records the original 14 measured textured variants plus the Bob-omb admission.  That is an appropriate first generic consumer, but it is materially short of the requested normal BOB actor set.

### Minor

1. The current plan states Task 8 is complete while the worktree contains a target-affecting, uncommitted cold-stage/prefix-return implementation.  The status must be reconciled before any commit/report says the narrow repair is complete.

2. The untracked capture files contain multiple historical runs.  Readers must use `.tmp-normal-bob-replay.json` (generation 22, Bob-omb family 7) rather than `.tmp-generic-bob-cart-probe.json` (older generation 15 without the Bob-omb witness).

### Discrepancies between summary and code

- The compressed summary’s “generic Bob-omb passed” is only partly sufficient: source/telemetry demonstrate a normally observed Bob-omb, but the checked image has not been accepted and no pixel oracle exists.
- A green host source-scene-bundle test does **not** exercise the current `main.c` sequence which returns the borrowed VDP1 bank; the production ordering is present but uncommitted and untested as an integration ordering.
- Any implication of working sound is false for the normal replay profile.  It explicitly links the no-op audio stub.
- Any implication of “complete BOB” is false.  The runtime’s own probe reports 15 variants across 47 families.

## What was done well

- The normal replay profile enables the generic closure path, and the current capture identifies the actual Bob-omb by family/model rather than relying on an injected test record.
- The cold upload has a bounded 2,560-byte phase borrow and the current source restores both frame command prefixes after it retires, rather than allocating another persistent HWRAM buffer.
- The focused host wave is genuinely green: scene-package schema/determinism, source scene bundle, and VDP1 painter-chain tests passed (27 tests plus 3 determinism tests, with two capability skips).
- The candidate manifest directly verifies against its required profile, so its artifact hashes are internally consistent even though it is intentionally not release-clean.

## Recommended next actions

1. Add the single narrow regression for the Task 8 cold-stage return ordering, commit only that repair plus its documentation, then rebuild and rerun the normal Bob-omb probe immediately.
2. Treat the next target work as visual correction on the existing generic route: fixed Mario Gouraud, correct VDP1 texture/material binding, and painter order.  Do not create another actor format, queue, or residency layer.
3. After a manually accepted normal Bob-omb frame, expand only through this already-proven resolver/emitter path.  Each additional actor should be a data/material admission, not an architecture sprint.
4. Replace `audio-stub-v1` with the existing audio path on this same BOB target and prove one music sequence plus one gameplay SFX before calling it a new demo.
5. Only then clean/rebuild/reproduce the candidate and reopen release/manual gates.

## Audit correction — 2026-08-13

The initial pass incorrectly classified the cold-stage ordering in `main.c` as
uncommitted implementation. The ordering itself landed in `dc81808b`; what was
missing was a direct regression that makes it load-bearing. The new
source-order regression fails against `dc81808b^` and passes at current source,
while the existing command-bank fixture proves prefix restoration after an
overwritten 2,560-byte prefix. This is a verification repair, not a new target
architecture or behavior change.

## Two-week process finding

From 2026-07-30 through the audited head, the repository contains 885 commits:
323 behavior commits, 387 documentation/test commits, and 175 other commits.
The actor work repeatedly followed the same loop: define a sealed host format,
complete a host parser/compiler, obtain a review finding at the memory or
target-consumer boundary, repair it, record evidence, then introduce the next
format or lifecycle layer before the first live normal-level render.  The
individual repairs are generally fail-closed and technically useful; the
failure is sequencing.  It optimized local proof and release evidence ahead of
the earliest Ymir/manual feedback.

The corrective operating rule is now concrete: no additional format, package,
or generic lifecycle change until the existing normal Bob-omb route visibly
renders correctly, then each BOB actor is admitted as data/material work on
that same route, and sound is connected through the already-present PCM/68k
path.  Any change that cannot be observed through that route is out of scope.

## Live-route audit addendum — 2026-08-13

- A fresh serial normal-BOB build produced identity
  `id-f49c32ac69e5385a`.  The ordinary BOB route then observed the normal
  `bhvBobomb` (`family_id=7`, `model_id=188`) at pool slot 67 through the
  generic resolver, with 68 Bob-omb output records, 12 textured commands,
  and 56 fixed/per-vertex Gouraud-lit untextured faces.  This is a real
  game-object path, not an injected actor.
- The checked live image remains **not accepted**.  It shows the generic
  Bob-omb route but also visibly broken terrain/VDP1 presentation.  The next
  task is to isolate that existing render fault on this exact route; it is not
  permission for another format, queue, or residency redesign.
- The Bob-omb's 11 baked CLUT16 tiles and five 16-entry palettes match the
  attested normal Bob-omb source art.  Its grayscale body is intentional in
  the original `bob-omb_left_side`/`right_side` inputs, rather than evidence
  of a missing palette upload.  The visual defect therefore must be proved at
  geometry/UV/order/presentation level before changing material policy.
- A narrow Mario-only correction was test-first verified in the same target:
  RGB1555 detail commands now inherit the existing fixed Gouraud table when
  their base primitive has one.  The live VDP1 list contains those detail
  commands in Gouraud mode with nonzero table addresses.  This fixes the
  specific missing-Mario-Gouraud defect, but does **not** make the screenshot
  acceptable or close the manual gate.
- The worktree currently has 91 modified tracked files spanning game, target,
  tooling, and documentation plus many historical probes.  No manual/demo or
  release claim is valid until the visual repair is isolated and its narrow
  change can be committed independently of that shared dirt.
