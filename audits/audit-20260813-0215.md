# Peer Audit — 2026-08-13 02:15

## Context

- Prior agent: Compacted Codex session.
- Task claimed: produce a normal, generic BOB build with the recent Mario/actor visual fixes and prepare it for a truthful Ymir/manual check.
- Files examined: `.tmp-normal-bob-replay-build.sh`, `Makefile.saturn.mk`, `src/port/saturn/sourceboot/Makefile`, `tools/saturn/release_manifest.py`, `src/port/saturn/gfx/saturn_hud_publish.[ch]`, `src/port/saturn/sourceboot/main.c`, `src/port/saturn/sourceboot/source_scene_bundle.c`, and the associated HUD/source-scene tests.
- Commands run: worktree/status/history inspection; current build-log and artifact inspection; `test_render_snapshot_source.py`; `verify-saturn-hud-layout`; its expected-failure mutation; and `verify-source-scene-bundle`.

## Findings

### Critical

1. The current normal BOB build is rejected by its own final sealing step, so the newly produced CUE is not yet a passing, identity-bound manual artifact.

   Evidence: `.tmp-normal-bob-replay-build.sh:12` changes into `/d/sm64-port`; on Windows this resolves to the `D:\\sm64-port` junction. `tools/saturn/release_manifest.py:148-162` correctly rejects a junction/reparse ancestor. The current build log ends in `ValueError: directory ancestor is a symlink, junction, or reparse point: D:\\sm64-port`, followed by `make: *** [Makefile.saturn.mk:263: sourceboot] Error 2`. The ELF, ISO, and CUE under `build/saturn/sourceboot/e2-bob-identity-id-808aaef39d5a34b9/` were written before that failure, but their producer command failed. No current Ymir boot or visual capture exists for this identity.

   Do not weaken `DirectoryNamespaceGuard`. The next repair must make the final manifest receive the real, non-reparse worktree root while retaining any safe short build paths needed by the toolchain, then rebuild and capture the resulting identity.

### Important

1. The profile used for this purported next demo explicitly disables semantic audio: `.tmp-normal-bob-replay-build.sh:19` sets `SATURN_FEATURE_SEMANTIC_AUDIO=0`. It cannot support a claim that music or triggered SFX work.

2. The live change set is not independently attributable. `git diff --name-only` reports 84 tracked paths, while the focused HUD/source-scene area alone mixes presentation changes with package-generation, generated-assembly-name, and identity-registry Make changes. The working build is therefore a mutable composite rather than a clean candidate. This is especially risky after the prior visual claims proved premature.

3. There is no current visual proof. The normal profile does correctly enable `SATURN_FEATURE_DYNAMIC_ACTOR_CLOSURE=1` and `SATURN_SOURCEBOOT_LIVE_INPUT=1`, and the link reached ISO/CUE creation, but neither fact proves Mario scale/Gouraud/order nor Bob-omb texture rendering. The required next evidence is a capture made only after the repaired build exits successfully and the normal scene has reached its rendered state.

### Minor

1. The stateless HUD comment is accurate about its bound: `SM64_SATURN_HUD_LAYOUT_MAX_CELLS` is 24 and the publisher clears a 20x14 grid before current glyphs. Focused tests pass and the mutation fails as intended. This is a targeted memory trade, not another renderer architecture; target presentation cost still needs the new Ymir run.

## Discrepancies between summary and code

1. The prior progress narrative said link/package creation had advanced and implied the next step was capture. The actual terminal state is a build failure at `seal-release`; an ISO/CUE merely exists as a partial by-product.

2. The requested manual-gate objective includes audio, but the exact normal build profile disables it.

3. The previous manual CUE cannot validate the current changes: the newly linked output tag is `id-808aaef39d5a34b9`, whereas the older manually exercised CUE predates the current uncommitted visual changes.

## What was done well

1. The generic normal profile does not use a Cannon injection: it enables live input and dynamic actor closure for BOB.

2. The HUD LWRAM correction is bounded and tested. `verify-saturn-hud-layout`, its clear-skip mutation, `test_render_snapshot_source.py`, and `verify-source-scene-bundle` all passed in this audit.

3. The namespace guard failed closed rather than allowing a manifest to be sealed through a junction. That is the correct security/reproducibility behavior.

## Recommended next actions

1. Repair only the path handoff at the final release-manifest boundary; preserve the junction/reparse rejection. Add a regression for short build paths plus a real canonical manifest root.

2. Rebuild the same normal BOB profile to a successful terminal exit, record its exact identity, then run an automated Ymir capture only after the level has rendered. Do not launch it as a new manual candidate before that evidence.

3. Make the visual gate binary: Mario correctly sized, fixed-table Gouraud visible, opaque ordering acceptable, and a normally spawned Bob-omb visibly uses its actor texture. If any fail, fix that one generic rendering path only.

4. Only after the visual gate passes, turn on one existing music sequence and one triggered SFX in the same normal profile. Do not start another package, format, or multi-level architecture task.

5. Commit only the repaired, verified slice with its changelog, plan, and evidence; keep unrelated dirty work out of that candidate.

## Follow-up verification (02:39–02:43)

The critical sealing failure above was repaired without weakening the namespace
guard.  Build paths may still use the short `D:\\sm64-port` junction, but the
release-manifest boundary now receives the physical worktree root and rebases
every declared input/output path beneath it.  Development provenance also
batches its closure `git status` calls below the Windows command-line limit.

- Focused regressions passed: canonical-manifest-root Make contract;
  long-closure development provenance batching; full `test_release_manifest`
  (29 tests); stateless HUD layout and its clear-skip mutation; render source
  policy; and source-scene bundle tests.
- The new normal BOB build exited successfully at identity
  `id-d970fb4b1ae7bc4d`.  Its manifest independently verifies as
  `43b0843b48065e87405b4b17f9e067945b550dca60537a1f5f45d5416964ccd4`.
- A fresh headless Ymir capture of that exact CUE reached source generation
  253.  Mario is visible in the resulting frame, and the live generic actor
  observer reports a rendered Bob-omb (`family_id=7`, `model_id=188`,
  `pool_slot=67`); no injected object path was used.

This remains a **development** artifact: the manifest truthfully records
`closure_clean=false` and `reproducibility=uncompared` because this is a
composite dirty worktree.  The screenshot is not yet sufficient visual proof
that the Bob-omb's texture/material and ordering are correct: the first live
Bob-omb witness is only at the edge of the frame.  Semantic audio is still
disabled in this profile.  Those gates remain open.
